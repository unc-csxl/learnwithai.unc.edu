{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Worker: Dramatiq",
      "type": "debugpy",
      "request": "launch",
      "module": "dramatiq",
      "args": [
        "learnwithai_jobqueue.worker",
        "--processes",
        "1",
        "--threads",
        "2"
      ],
      "cwd": "${workspaceFolder:job_queue}",
      "envFile": "${workspaceFolder}/.env",
      "env": {
        "PYTHONPATH": "${workspaceFolder:job_queue}/src",
        "RABBITMQ_URL": "amqp://guest:guest@rabbitmq:5672/"
      },
      "justMyCode": true
    }
  ]
}