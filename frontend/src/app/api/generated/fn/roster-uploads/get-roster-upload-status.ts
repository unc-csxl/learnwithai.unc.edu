/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RosterUploadStatusResponse } from '../../models/roster-upload-status-response';

export interface GetRosterUploadStatus$Params {
  job_id: number;
  course_id: number;
}

export function getRosterUploadStatus(http: HttpClient, rootUrl: string, params: GetRosterUploadStatus$Params, context?: HttpContext): Observable<StrictHttpResponse<RosterUploadStatusResponse>> {
  const rb = new RequestBuilder(rootUrl, getRosterUploadStatus.PATH, 'get');
  if (params) {
    rb.path('job_id', params.job_id, {});
    rb.path('course_id', params.course_id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RosterUploadStatusResponse>;
    })
  );
}

getRosterUploadStatus.PATH = '/api/courses/{course_id}/roster-uploads/{job_id}';
