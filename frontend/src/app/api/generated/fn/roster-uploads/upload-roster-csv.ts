/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BodyUploadRosterCsv } from '../../models/body-upload-roster-csv';
import { RosterUploadResponse } from '../../models/roster-upload-response';

export interface UploadRosterCsv$Params {
  course_id: number;
      body: BodyUploadRosterCsv
}

export function uploadRosterCsv(http: HttpClient, rootUrl: string, params: UploadRosterCsv$Params, context?: HttpContext): Observable<StrictHttpResponse<RosterUploadResponse>> {
  const rb = new RequestBuilder(rootUrl, uploadRosterCsv.PATH, 'post');
  if (params) {
    rb.path('course_id', params.course_id, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RosterUploadResponse>;
    })
  );
}

uploadRosterCsv.PATH = '/api/courses/{course_id}/roster-uploads';
