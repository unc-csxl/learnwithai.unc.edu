/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MembershipResponse } from '../../models/membership-response';
import { UpdateMemberRoleRequest } from '../../models/update-member-role-request';

export interface UpdateMemberRole$Params {
  course_id: number;
  pid: number;
      body: UpdateMemberRoleRequest
}

export function updateMemberRole(http: HttpClient, rootUrl: string, params: UpdateMemberRole$Params, context?: HttpContext): Observable<StrictHttpResponse<MembershipResponse>> {
  const rb = new RequestBuilder(rootUrl, updateMemberRole.PATH, 'patch');
  if (params) {
    rb.path('course_id', params.course_id, {});
    rb.path('pid', params.pid, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MembershipResponse>;
    })
  );
}

updateMemberRole.PATH = '/api/courses/{course_id}/members/{pid}';
