/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateIyowActivityRequest } from '../../models/create-iyow-activity-request';
import { IyowActivityResponse } from '../../models/iyow-activity-response';

export interface CreateIyowActivity$Params {
  course_id: number;
      body: CreateIyowActivityRequest
}

export function createIyowActivity(http: HttpClient, rootUrl: string, params: CreateIyowActivity$Params, context?: HttpContext): Observable<StrictHttpResponse<IyowActivityResponse>> {
  const rb = new RequestBuilder(rootUrl, createIyowActivity.PATH, 'post');
  if (params) {
    rb.path('course_id', params.course_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IyowActivityResponse>;
    })
  );
}

createIyowActivity.PATH = '/api/courses/{course_id}/activities/iyow';
