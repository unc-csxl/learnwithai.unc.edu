/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { IyowActivityResponse } from '../../models/iyow-activity-response';
import { UpdateIyowActivityRequest } from '../../models/update-iyow-activity-request';

export interface UpdateIyowActivity$Params {
  course_id: number;
  activity_id: number;
      body: UpdateIyowActivityRequest
}

export function updateIyowActivity(http: HttpClient, rootUrl: string, params: UpdateIyowActivity$Params, context?: HttpContext): Observable<StrictHttpResponse<IyowActivityResponse>> {
  const rb = new RequestBuilder(rootUrl, updateIyowActivity.PATH, 'put');
  if (params) {
    rb.path('course_id', params.course_id, {});
    rb.path('activity_id', params.activity_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IyowActivityResponse>;
    })
  );
}

updateIyowActivity.PATH = '/api/courses/{course_id}/activities/{activity_id}';
