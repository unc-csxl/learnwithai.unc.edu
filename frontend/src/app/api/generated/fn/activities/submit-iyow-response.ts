/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { IyowSubmissionResponse } from '../../models/iyow-submission-response';
import { SubmitIyowRequest } from '../../models/submit-iyow-request';

export interface SubmitIyowResponse$Params {
  course_id: number;
  activity_id: number;
      body: SubmitIyowRequest
}

export function submitIyowResponse(http: HttpClient, rootUrl: string, params: SubmitIyowResponse$Params, context?: HttpContext): Observable<StrictHttpResponse<IyowSubmissionResponse>> {
  const rb = new RequestBuilder(rootUrl, submitIyowResponse.PATH, 'post');
  if (params) {
    rb.path('course_id', params.course_id, {});
    rb.path('activity_id', params.activity_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IyowSubmissionResponse>;
    })
  );
}

submitIyowResponse.PATH = '/api/courses/{course_id}/activities/{activity_id}/submissions';
