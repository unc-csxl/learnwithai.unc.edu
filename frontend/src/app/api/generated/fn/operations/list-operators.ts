/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OperatorResponse } from '../../models/operator-response';

export interface ListOperators$Params {
}

export function listOperators(http: HttpClient, rootUrl: string, params?: ListOperators$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<OperatorResponse>>> {
  const rb = new RequestBuilder(rootUrl, listOperators.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<OperatorResponse>>;
    })
  );
}

listOperators.PATH = '/api/operations/operators';
