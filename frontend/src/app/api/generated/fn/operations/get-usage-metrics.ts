/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UsageMetricsResponse } from '../../models/usage-metrics-response';

export interface GetUsageMetrics$Params {
}

export function getUsageMetrics(http: HttpClient, rootUrl: string, params?: GetUsageMetrics$Params, context?: HttpContext): Observable<StrictHttpResponse<UsageMetricsResponse>> {
  const rb = new RequestBuilder(rootUrl, getUsageMetrics.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<UsageMetricsResponse>;
    })
  );
}

getUsageMetrics.PATH = '/api/operations/metrics';
