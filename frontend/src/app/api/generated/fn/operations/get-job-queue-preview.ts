/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QueueMessagePreviewResponse } from '../../models/queue-message-preview-response';

export interface GetJobQueuePreview$Params {
  queue_name: string;
  limit?: number;
  page?: number;
}

export function getJobQueuePreview(http: HttpClient, rootUrl: string, params: GetJobQueuePreview$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<QueueMessagePreviewResponse>>> {
  const rb = new RequestBuilder(rootUrl, getJobQueuePreview.PATH, 'get');
  if (params) {
    rb.path('queue_name', params.queue_name, {});
    rb.query('limit', params.limit, {});
    rb.query('page', params.page, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<QueueMessagePreviewResponse>>;
    })
  );
}

getJobQueuePreview.PATH = '/api/operations/jobs/queues/{queue_name}/preview';
